# emails — Needs-Fix Endpoints

- DELETE /emails/builder/{locationId}/{templateId} (Version: 2021-07-28) — type: other — notes: Parameter token mismatch vs client: DELETE /emails/builder/{this.config.locationId}/{templateId}
